spring:
  application:
    name: consumer
  cloud:
    bus:
      enabled: true
      trace:
        enabled: true

server:
  port: 8081

feign:
  hystrix:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream, bus-refresh
