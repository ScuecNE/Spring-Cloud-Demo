spring:
  application:
    name: consumer
  cloud:
    bus:
      enabled: true
      trace:
        enabled: true
  security:
    user:
      name: "client"
      password: "client"

server:
  port: 10712

feign:
  hystrix:
    enabled: true
