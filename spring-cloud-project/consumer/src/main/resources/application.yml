spring:
  application:
    name: consumer

server:
  port: 8081
  servlet:
    context-path: /consumer

feign:
  hystrix:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream, refresh
